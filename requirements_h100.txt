# H100-optimized training requirements
# Install with: pip install -r requirements_h100.txt

# Core dependencies
torch>=2.1.0
numpy>=1.24.0
einops>=0.7.0
tqdm>=4.65.0

# Data handling
# (Using local preprocessed numpy files)

# Mixed precision and optimization
accelerate>=0.24.0

# Logging
wandb>=0.15.0
tensorboard>=2.14.0

# GPU monitoring
pynvml>=11.5.0

# Optional H100 Optimizations (install separately after base requirements)
# ========================================================================

# 1. Flash Attention - Efficient attention computation
#    IMPORTANT: Requires torch to be installed first!
#    Install with:
#      pip install torch  # Install torch first
#      pip install flash-attn --no-build-isolation
#    
#    If you encounter memory errors during compilation:
#      MAX_JOBS=1 pip install flash-attn --no-build-isolation

# 2. NVIDIA Transformer Engine - FP8 support for H100
#    Requires: CUDA >= 12.0, GPU compute capability >= 8.9
#    Install with:
#      pip install transformer-engine
#    Or from source:
#      pip install git+https://github.com/NVIDIA/TransformerEngine.git

# For development
pytest>=7.4.0
ruff>=0.1.0